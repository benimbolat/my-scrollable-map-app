import { ButtonProps } from "react-native";

export interface ButtonAtomTypes extends ButtonProps {
    title: string;
}

// default props
// export const defaultProps: ButtonAtomTypes = {
//     title: "default",
// };
